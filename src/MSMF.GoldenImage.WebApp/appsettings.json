{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Msmf": {
    "//": "Value below is replaced per environment by the Octopus JSON configuration variables feature (matches project variable 'Msmf:Environment').",
    "Environment": "Local (unreplaced default)"
  }
}
